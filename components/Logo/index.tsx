import { LogoWrapper, LogoTitle, LogoSubTitle } from './Logo.styles';

export default function Logo() {
    return (
        <LogoWrapper>
            <LogoTitle>P&nP</LogoTitle>
            <LogoSubTitle>posts and new posts</LogoSubTitle>
        </LogoWrapper>
    );
}
